using System;

namespace Inscryption_Server.DataTypes
{
	public abstract class Sigil
	{
		internal static Sigil[] GetSigils(string[] sigils)
		{
			if(sigils == null)
			return new Sigil[0];

			return new Sigil[0];
		}
	}
}
